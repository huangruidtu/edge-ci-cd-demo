version: 2

updates:
  # 🐳 Docker（你的项目最重要）
  - package-ecosystem: "docker"
    directory: "/"
    schedule:
      interval: "daily"

  # ⚙️ GitHub Actions（CI依赖）
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "daily"
